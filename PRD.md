# Record of Life — PRD

## 1. 목적

필름 사진 촬영 시 카메라가 자동 기록하지 못하는 촬영 메타데이터(조리개, 셔터, ISO, 렌즈 등)를 야외에서 즉시 기록하고, 나중에 인화·스캔한 디지털 파일에 EXIF로 주입할 수 있도록 Export한다.

이 앱의 스코프는 **기록 + Export까지**. EXIF 주입은 별도 앱/도구(exiftool 등)가 담당.

## 2. 사용자와 상황

- 필름 카메라 사용자 (35mm/120 등)
- 야외, 종종 겨울·장갑 착용, 한 손 조작
- 촬영 후 스캔본을 받으면 촬영 정보와 매칭해 EXIF 주입

## 3. 핵심 원칙

1. **야외 UX 최우선**: 장갑 낀 손 한 개로 3초 안에 한 샷 저장 가능해야 함
2. **기록 손실 금지**: 앱 재시작·강제 종료 후에도 데이터 유지
3. **Export가 반드시 존재**: 기록만 하고 못 꺼내면 앱의 존재 이유 없음
4. **EXIF 표준 정합**: Export 필드명은 EXIF/exiftool 표준 태그와 매칭 가능해야 함

## 4. 기능 정의

### 4.1 롤 관리

- 새 롤 생성: 카메라, 필름, 총 프레임 수(기본 36), 시작일, 제목/메모(선택)
- 롤 목록: 진행 중 / 완료 필터
- 롤 상태: 진행 중, 완료
- 롤 완료 처리 (수동)
- 롤 삭제

### 4.2 카메라 / 렌즈 / 필름 마스터 데이터

- 카메라 등록: 이름, 브랜드, 포맷(35mm/120 등)
- 렌즈 등록: 이름, 초점거리(고정 또는 줌 범위), 최대 조리개
- 필름 등록: 이름, 브랜드, ISO, 포맷
- 각각 목록·수정·삭제
- 롤에 카메라·필름을 결합. 렌즈는 롤 기본값 + 샷별 override 가능

### 4.3 샷 기록 (핵심)

한 샷당 기록 항목:

| 필드 | 필수 | EXIF 대응 | 비고 |
|------|------|-----------|------|
| 프레임 번호 | 필수 | (매칭 키) | 스캔 파일 매칭용, 자동 증가 |
| 촬영 일시 | 필수 | DateTimeOriginal | 기본값: 저장 시각 |
| 조리개 (f-stop) | 필수 | FNumber | |
| 셔터 스피드 | 필수 | ExposureTime | |
| 렌즈 | 선택 | LensModel | 롤 기본값 상속 |
| 초점거리 | 선택 | FocalLength | 줌 렌즈면 필수, 단렌즈면 렌즈에서 자동 |
| ISO (실제 노출) | 선택 | ISO | 기본: 필름 ISO. push/pull 시 override |
| 노출 보정 | 선택 | ExposureBiasValue | ±EV |
| GPS 좌표 | 선택 | GPSLatitude/Longitude | 저장 시 자동, 사용자 허용 시 |
| 평점 | 선택 | Rating | 나중 편집용 |
| 메모 | 선택 | ImageDescription | |
| 참고 사진 | 선택 | — | 스캔 매칭용 스냅샷, EXIF 대상 아님 |

**야외 최소 입력**: 조리개, 셔터, (필요 시) 노출 보정. 나머지는 롤 기본값 상속 또는 자동.

### 4.4 야외 입력 UX

- **입력 모드 (Sticky Capture Mode, 핵심)**: 롤에서 "입력 시작" 진입 시, 뒤로가기를 누르기 전까지 이 화면에 계속 머문다. 사용자가 필수값 입력 후 "확인" 누르면:
  1. 현재 값으로 샷 저장
  2. 롤의 샷 카운트 +1 (`shotsDone`)
  3. **화면 전환 없음** — 같은 페이지 유지
  4. 필드는 방금 저장한 값 그대로 남아 다음 샷 입력 대기
  5. 짧은 확인 피드백 (햅틱 + 프레임 번호 갱신 표시)

  뒤로가기 눌러야만 롤 상세 화면으로 복귀. 연속 촬영 중 "저장 → 새 샷 버튼 → 페이지 재진입"의 반복 마찰을 제거.

  샷 카운트가 롤의 총 프레임 수에 도달하면 자동으로 완료 안내를 띄우되, 사용자가 계속 진행하기(총 매수 초과 촬영)를 선택할 수 있음.

- **직전 값 유지**: 입력 모드에서는 자연스럽게 직전 샷 값이 남아 있음. 카메라 세팅이 안 바뀌는 대다수 케이스에서 "확인"만 반복해도 유효한 기록이 쌓임. 값을 바꿀 때는 해당 필드 하나만 탭해서 조정. **롤의 첫 샷 진입 시** 필드는 롤 기본값(필름 ISO, 렌즈 기본값) 상속, 나머지는 비어 있음.
- **큰 그리드 셀렉터**: 조리개/셔터는 가로 스크롤이 아닌 큰 정사각 버튼 그리드. 장갑 친화.
- **손잡이 설정**: 최초 실행 시 왼손/오른손 선택. 저장 버튼·그리드 자주 쓰는 값·하단 시트 액션이 엄지 사거리에 배치.
- **햅틱**: 모든 selection에 진동 피드백. 시각 확인 없이 조작 인지.
- **한 손 조작 보장**: 주요 액션은 화면 하단 3분의 1 안.
- **자동 프레임 번호**: 저장 시 현재 롤의 다음 번호 자동 부여. 수동 override 가능.
- **자동 촬영 시각**: 저장 시 `now()`. 편집 가능.
- **자동 GPS**: 권한 허용 시 저장 시점 좌표. 정확도 낮으면 무시.

**야외 모드 밖 편집**: 평점, 메모, 참고 사진, 렌즈 override는 나중에 실내에서.

### 4.5 Export

- 대상: 하나 이상의 롤 선택
- 포맷: **exiftool 호환 JSON** (기본)
  - 배열 형태, 각 항목이 한 샷
  - `SourceFile` 필드에 스캔 파일 매칭 규칙 적용 (예: `{roll_id}_{frame:03d}.jpg`)
  - EXIF 표준 태그명 사용 (`FNumber`, `ExposureTime`, `ISO`, `FocalLength`, `LensModel`, `DateTimeOriginal`, `GPSLatitude`, `GPSLongitude`, `ImageDescription`, `Rating`)
- 파일명 규칙: 사용자 설정 가능 (기본 `{roll_id}_{frame:03d}`)
- 공유: OS 표준 공유 시트 (이메일, 클라우드, AirDrop 등)
- 사용법: 사용자가 스캔본 폴더에서 `exiftool -json=export.json *.jpg` 실행하면 주입 완료

### 4.6 손잡이 설정

- 최초 실행 온보딩 1페이지: "폰을 어느 손으로 잡나요?" → 왼손 / 오른손
- 설정 화면에서 언제든 변경

## 5. 스코프 아웃

- **EXIF 실제 주입** — 별도 앱/exiftool 사용
- **필름 스캔 기능** — 별도 스캐너·앱 사용
- **AI 인식·자동 태깅** — YAGNI
- **다중 사용자·클라우드 동기화** — 개인 로컬 앱
- **볼륨 버튼 하이재킹** — 검토 결과 iOS 사실상 불가, 실효 낮음
- **음성 입력** — 겨울 마스크·소음 환경에 미덥지 않음
- **필터·ND** — 필요해지면 추가

## 6. 데이터 영속성

- 로컬 저장. 앱 재시작 후 유지 필수.
- 저장 매체는 기술 결정 사항 (본 PRD 스코프 아님).

## 7. 성공 기준

1. 장갑 낀 손 하나로 한 샷을 3초 안에 저장 가능
2. 앱 강제 종료 후에도 모든 롤·샷 유지
3. Export한 JSON을 `exiftool -json=` 로 실제 이미지 파일에 주입 성공
4. 한 롤(36샷) 야외 촬영 중 배터리·앱 이슈 없이 완주

## 8. 우선순위 로드맵

1. **데이터 영속성** — 없으면 앱이 무의미
2. **프레임 번호 필드** — Export 매칭 키
3. **Export (exiftool JSON)** — 앱 존재 이유의 완성
4. **야외 UX 개편** — 그리드 셀렉터, preset 상속, 손잡이 설정, 햅틱
5. **GPS 자동 기록** — EXIF 위치 정보
6. **촬영 시각 (시:분)** — EXIF DateTimeOriginal 완전화
7. **초점거리·push/pull ISO** — 줌 렌즈·현상 변주 대응
